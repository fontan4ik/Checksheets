// ============================================
// УТИЛИТЫ И ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ
// ============================================

/**
 * Очистка строки от лишних символов (для артикулов)
 */
function cleanArticle(art) {
  return art ? String(art).trim() : "";
}
