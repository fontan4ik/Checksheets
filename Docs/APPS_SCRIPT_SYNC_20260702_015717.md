# Apps Script cloud → local sync — 2026-07-02 01:57 +04

## Scope

Обновление локальной папки Checksheets актуальными файлами из Google Apps Script project **«Главные скрипты v2»**.

Направление операции:

```text
Google Apps Script cloud → local repo
```

Обратной записи в Google не было.

## IDs

| Объект | Значение |
| --- | --- |
| Spreadsheet ID | `15d_fAFFFAoBE_ClIhzDxwjRW2IeDFCKpbcqyQapyKhI` |
| Apps Script script ID | `13r5qTN_nb0F9yM47t5PmrXMtCvqGz0CoedPxrv8AMatMJCgdSxYw4dSl` |
| Local project | `/Users/vladimirgrebennikov/Code/Checksheets_Project/Checksheets` |

## Source export

Cloud export был получен из открытого Google Apps Script editor через Monaco editor models.

- Export copy: `test/tmp/apps-script-cloud-export-20260701T215717Z.json`
- Local backup before overwrite: `test/tmp/apps-script-local-backup-20260701T215717Z/`
- Machine summary: `test/tmp/apps-script-sync-summary-20260701T215717Z.json`

## Result

| Metric | Value |
| --- | ---: |
| Cloud files exported | `49` |
| Created locally | `0` |
| Updated locally | `0` |
| Already unchanged | `49` |
| Missing after sync | `0` |
| Mismatched after sync | `0` |
| Extra local `.gs` not in cloud | `13` |

Вывод: локальная папка уже была синхронизирована с cloud baseline; повторный pull подтвердил, что все 49 cloud-файлов локально присутствуют и совпадают byte-for-byte.

## Extra local `.gs` files not present in cloud

Эти файлы оставлены на месте и **не удалялись**:

- `Feron API.gs`
- `Ozon FBO FBS продажи.gs`
- `Ozon реклама V2.gs`
- `Ozon реклама V3.gs`
- `WB FBO FBS продажи.gs`
- `ВБ продажа FBO FBS.gs`
- `Диагностика RS.gs`
- `Новые колонки Продаж.gs`
- `Новые колонки итог.gs`
- `Синхронизация остатков ETM TR.gs`
- `Синхронизация остатков Feron.gs`
- `Синхронизация остатков ODC.gs`
- `Синхронизация остатков.gs`

## Clasp binding

`.clasp.json` сохранён/обновлён без секретов:

```json
{
  "scriptId": "13r5qTN_nb0F9yM47t5PmrXMtCvqGz0CoedPxrv8AMatMJCgdSxYw4dSl",
  "rootDir": "."
}
```

`.claspignore` оставлен в безопасном режиме, чтобы случайный `clasp push` не загрузил весь репозиторий:

```text
**/**
!*.gs
!appsscript.json
```

## Safety

Не выполнялось:

- `clasp push`;
- Apps Script API `projects.updateContent`;
- запись в Google Apps Script cloud;
- удаление локальных extra `.gs` файлов;
- изменение launchd/jobs/triggers;
- marketplace write scripts;
- process kill/restart.

## Rollback

Если нужно вернуть локальные `.gs`/clasp config к состоянию до этой операции:

```text
test/tmp/apps-script-local-backup-20260701T215717Z/
```

скопировать файлы из backup обратно в корень проекта.

## Verification

Focused ad-hoc verification должен проверить:

- export существует;
- script ID корректный;
- в export 49 файлов;
- каждый cloud file существует локально;
- все локальные cloud files byte-for-byte совпадают с export;
- `.clasp.json` и `.claspignore` корректны;
- report содержит backup/export/summary paths;
- secret fragments не попали в report.
