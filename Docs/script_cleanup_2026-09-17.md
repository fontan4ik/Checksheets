# Сверка скриптов и триггеров — 17.09.2026

Источник расписания: список триггеров, переданный владельцем проекта. Список учитывает триггеры другого пользователя, даже если они временно отключены. `appsscript.json` содержит настройки проекта, а не расписание триггеров; `.claspignore` определяет загрузку файлов.

| Колонки / лист | Оставленный обработчик | Удалённый дубль |
| --- | --- | --- |
| `тест!AQ:AT` (43–46) | `updateOzonFBOSales()` в `Ozon продажи FBO FBS.js` | `Ozon FBO FBS продажи.js` — та же функция и алгоритм |
| `тест!AV:AY` (48–51) | `updateWBFBOFBSSales()` в `ВБ продажи FBO FBS.js` | `WB FBO FBS продажи.js` — побайтовая копия; `ВБ продажа FBO FBS.js` — старый альтернативный расчёт |
| `тест!BA:BC` (53–55) | `updateOzonAdPerfFinal()` в `Ozon реклама V3.js` и локальный `ozon_perf_sync.py` | `Ozon реклама V2.js` — прежняя запись тех же колонок |
| `тест!BH:BI` (60–61) | `updateOzonCancellationsAndReturns()` в `Ozon возвраты и отмены.js` | `updateOzonCancellations()` удалена из файла продаж Ozon: повторная запись BH |
| `тест!O` (15) | `updateWBStocksFromStatisticsAPI()` как шаг `WbMain` | `Chrlid.js` — альтернативная запись O; триггера у неё нет |

Также удалены две побайтовые копии устаревшей обёртки `Новые колонки ...`, побайтовая копия диагностики RS (`Без названия 2.js`) и пустой legacy-файл `Синхронизация остатков.js`. Оставлена именованная `Диагностика RS.js`.

## Проверка копии TZContent2

Копия не равна текущему рабочему проекту. В ней отсутствуют и уникальные модули, которые следует сохранить:

- `НТЦ списания.js` — отдельный лист `НТЦ списания` и журнал резерва, не дублирует основную таблицу.
- `Ozon реклама V3.js` — последняя реализация BA:BC; при этом в переданном списке триггер для неё отсутствует. Сохранена как ручной/резервный путь, а не как подтверждённый активный триггер.
- `Ozon реклама.js` — запись AU (47), не совпадает с BA:BC.
- `Alright API.js` — импорт остатков транзисторов в другой лист.
- `stream_supps_schema.js` и `sync-rs-stocks.js` — локальные Node-модули. При повторной проверке выяснилось, что первый из них всё же загружался в Apps Script; он добавлен в `.claspignore`.

Триггерные `updateOzonFBOSales()` и `updateWBFBOFBSSales()` в копии остались доступны в файлах с другими именами; удалённые там одноимённые файлы действительно были дублями. Файл `Ozon продажи FBO FBS.js` дополнительно содержал конкурирующую запись BH, устранённую здесь.

## Повторная проверка колонок и листов

Заголовки листа `ТЕСТ` сверены чтением таблицы 17.09.2026. Исправлены дополнительные пересечения:

- `ТЕСТ!G` (7) — теперь пишет только `updateAllFBSStocks()` суммой всех FBS складов. `getStocksByWarehouseFBS()` пишет только `ТЕСТ!H` (8), остаток Москвы. До исправления последняя функция перезаписывала G остатками *без* Москвы.
- `ТЕСТ!Q` (17) — оставлена одна `updateWBFBSMoscow()` в `ВБ.js`. Она использует существующий запрос по `chrtId` к WB-складу `1449484`; конкурирующий `WB FBS Москва.js` удалён.
- `ТЕСТ!O` (15) — старый ручной `mainV2()` и его альтернативный расчёт FBO удалены из `ВБ.js`; остаётся обработчик `updateWBStocksFromStatisticsAPI()` в цепочке `WbMain`.
- `ТЕСТ!L` (12) — удалён невызываемый `importStocksWithImages()`, который мог записать WB-остаток поверх суммы заказов Ozon.
- `ТЕСТ!N` (14) — удалён `updateOrdersSummaryOld()`; `WbMain` вызывает `updateOrdersSummaryV2()`.
- `ТЕСТ!S` (19) — удалён `Ozon Запись SKU.js`: его ручная функция писала Ozon SKU в колонку квартальной аналитики WB. Действующий `updateProductsV2()` пишет SKU в V (22).
- `ТЕСТ!Z:AA` (26–27) — `updateWBWarehousesFromStatisticsAPI()` оставлена как явно названный резервный ручной путь для `updateWBWarehousesByName()`; автоматического второго запуска в переданном расписании нет.
- `ТЕСТ!U` (21) — `updateProductsV2()` вызывает общий `syncOfferIdWithProductIdCore_()`, а отдельный триггер `syncOfferIdWithProductId()` использует тот же core и lock. Два запуска из расписания избыточны, но оба сохранены: удаление чужого триггера не входит в эту чистку.
- `ТЕСТ!AU` (47) и `ТЕСТ!BA:BC` (53–55) — разные показатели рекламы; `Ozon реклама.js` и `Ozon реклама V3.js` оставлены.
- `UNIT API` и `UNIT API квартал` — разные листы для `O.js` и `O квартал.js`, несмотря на похожую структуру кода.
- `utils.js` удалён: он целиком повторял имена `retryFetch()` и `rateLimitRPS()` из `fetchapp.js`, но иначе обрабатывал HTTP 429 и задержки. Единый обработчик теперь находится в `fetchapp.js`.
- Одинаковый детектор WB-ошибки в ARL и RS получил отдельные имена для каждого поставщика. Это устраняет конфликт глобальных функций без изменения их алгоритма.

## Оставшиеся вопросы

- `WbMain` и `OzonMain` других пользователей в переданном списке временно отключены. Сохранены их функции и зависимости, чтобы повторное включение триггеров не оборвалось из-за этой чистки.
- `updateOzonFBOSales`, `updateOzonReviewCountBL` и `startFetchAndWriteAnalytics` имеют 100% ошибок по переданному снимку триггеров. Эта чистка не устраняет причины ошибок; удалять их как «неиспользуемые» нельзя.
- В `appsscript.json` нет списка триггеров. Сверка по коду не подтверждает успешное выполнение API-запросов.
