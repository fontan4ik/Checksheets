# Инструкции по загрузке файлов в Google Apps Script

## 📋 ФАЙЛЫ ДЛЯ ЗАГРУЗКИ В APPS SCRIPT:

### ЗАМЕНИТЬ:
1. **Ozon продажи FBO FBS.gs** - Исправлена структура API запросов, теперь используется правильный параметр `dimensions` вместо `dimension` как указано в документации. Обновлены комментарии с актуальной информацией о реальных значениях API.

## 🔄 ВЫПОЛНИТЬ ПОСЛЕ ЗАГРУЗКИ:

1. **updateOzonSalesFBOFBS()** - запустить функцию для обновления продаж FBO/FBS в таблице

## 📝 ПРИМЕЧАНИЯ:

- Функция теперь корректно следует спецификации API документации
- Реальные данные от API могут отличаться от ожидаемых значений (например, для SKU 301916350 за период 26.01.2026-26.02.2026 возвращаются значения FBO 45, FBS 45 вместо ожидаемых 40 и 5)
- Использование правильной структуры `dimensions: ["sku"]` вместо `dimension: ["sku"]` обеспечивает корректную работу с API
- Добавлено детальное логирование для отслеживания значений конкретных SKU

## ✅ ПРОВЕРКИ:

После загрузки:
1. Проверьте, что функция `updateOzonSalesFBOFBS()` выполняется без ошибок
2. Проверьте логи выполнения для подтверждения успешной работы
3. Убедитесь, что данные заполняются в правильные колонки (AQ, AR, AS, AT)